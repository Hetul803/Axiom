# Axiom release plan

1. Re-inspect current repository and rerun baseline tests.
2. Re-attempt official starter inspection and record exact network status; vendor only non-secret interface notes if cloning is blocked.
3. Improve the shared core without discarding working pieces: richer perception summaries, action-semantic posterior, expanded executable DSL operations, particle ESS/resampling/deduplication, goal progress, A*/best-first planning, deadline-safe runtime diagnostics, and snapshot/restore SDK support.
4. Add official-agent release form, competition release builder, compact embedded source bundle, expanded cells, manifest, checklist, runtime report, public-results schema, and blocked-run report when official runtime is unavailable.
5. Add startup SDK capabilities: snapshot/restore, approval controls for destructive actions, browser-sandbox simulator, replay/audit trail, benchmark comparison, and documentation assets.
6. Expand tests beyond 150 meaningful cases across core, competition packaging/release, startup SDK/safety, runtime deadlines, and property-style invariants.
7. Run required commands, record real measured results only, commit, and create PR metadata.
