[project]
name = "axiom-arc-agent"
version = "0.1.0"
description = "Deterministic causal world-model agent for ARC-AGI-3 style environments"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
