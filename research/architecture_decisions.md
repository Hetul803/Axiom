# Architecture decisions

- Keep the competition runtime offline, deterministic, and CPU-first.
- Keep optional proposal interfaces disabled until public-game evidence justifies them.
- Use executable symbolic hypotheses as the authority for action selection.
- Separate public-game reporting from procedural or startup-demo metrics.
