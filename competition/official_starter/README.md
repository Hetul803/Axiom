# Official starter reference

The repository attempted to fetch `https://github.com/arcprize/ARC-AGI-3-Kaggle-Starter` with `git ls-remote` and GitHub API calls. Both direct Git operations and API calls are blocked in this execution environment by a 403 CONNECT tunnel. The official interface observations are recorded in `competition/OFFICIAL_STARTER_VERSION.md` and are used by `competition/official_agent/my_agent.py`.

No secrets, downloaded game caches, Kaggle credentials, or environment files are stored here.
